import styled from "styled-components";
import { StyledContainer } from "./Container";

export const StyledChatContainer = styled(StyledContainer)`
    @media (max-width: 600px) {
        height: 68vh;
    }
`